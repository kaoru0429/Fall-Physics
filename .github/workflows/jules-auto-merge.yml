name: Jules Auto-Merge Workflow

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    if: |
      github.event.pull_request.user.login == 'jules[bot]' || 
      contains(github.event.pull_request.labels.*.name, 'auto-merge')
    
    steps:
      - name: 📝 通知開始處理
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🤖 自動合併流程啟動中...\n⏳ 正在等待所有檢查完成'
            })

      - name: ⏳ 等待 Vercel 部署
        uses: patrickedqvist/wait-for-vercel-preview@v1.3.1
        id: vercel-preview
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          max_timeout: 300

      - name: �� 測試預覽網址
        run: |
          echo "Testing preview URL: ${{ steps.vercel-preview.outputs.url }}"
          curl -f "${{ steps.vercel-preview.outputs.url }}" || exit 1
          echo "✅ Homepage OK"
          curl -f "${{ steps.vercel-preview.outputs.url }}/chapter/00-tutorial" || exit 1
          echo "✅ Chapter page OK"

      - name: ✅ 啟用自動合併
        run: |
          gh pr merge --auto --squash "${{ github.event.pull_request.number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🎉 通知合併成功
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `✅ **PR 已自動合併！**
              
              🚀 Vercel 正在部署到正式環境
              🔗 正式網址：https://fall-physics-mu.vercel.app
              
              預覽測試結果：
              - ✅ 首頁載入正常
              - ✅ 章節頁面正常
              
              _此 PR 由 Jules 自動合併_`
            })

      - name: ❌ 通知失敗
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ **自動合併失敗**\n\n請檢查 Actions 日誌查看錯誤原因。'
            })
